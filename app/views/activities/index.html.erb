<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>The Good Tracker</title>

    
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="container">
      <div class="header clearfix">
        <nav>
          <ul class="nav nav-pills pull-right">
            <li role="presentation" class="active"><a href="#">Home</a></li>
            <li role="presentation"><a href="#">About</a></li>
            <li role="presentation"><a href="#">Contact</a></li>
          </ul>
        </nav>
        <h3 class="text-muted">The Good Tracker</h3>
      </div>

      <div class="jumbotron">
        <h1>Track your Hours !!!</h1>
        <p class="lead">Good tracker gives you a picture of where your time is spent at weekly / monthly basis and extrapolates the info to say where you ll be in a year.</p>
        <p><a class="btn btn-lg btn-success" href="#" role="button">Sign up today</a></p>
      </div>

      <div class="row marketing">
        <div class="col-lg-4">
          <h4>Enter your activity :</h4>
          <%= render 'form' %>
        </div>

        <div class="col-lg-4">
          <h4>Your current activity :</h4>
            <% if @current_activity.nil? %>
                No Current activity.
            <% else %>
                <%= render 'show', activity: @current_activity %>
            <% end %>
        </div>

        <div class="col-lg-4">
          <h4>Your list of open activities :</h4>
            <% if @all_open_activity.nil? || @all_open_activity.size == 0 %>
                No Open activities.
            <% else %>
                <% @all_open_activity.each {|act| %> <%= render 'show', activity: act %> <% } %>
            <% end %>
        </div>
      </div>

      <footer class="footer">
        <p>&copy; 2015 Company, Inc.</p>
      </footer>

    </div> <!-- /container -->
  </body>
</html>


